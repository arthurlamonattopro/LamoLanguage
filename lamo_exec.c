// Código gerado por Lamo v2 (via AST)
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int add(int a, int b);

int add(int a, int b) {
    return a + b;
}

int main() {
    int base = 5;
    printf("%d\n", add(base, 7));
    return 0;
}

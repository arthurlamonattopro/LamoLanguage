let global = 10;

fn use_scope(value) {
    let local = value + global;
    if (local > 10) {
        let inner = local + 1;
        print(inner);
    } else {
        print(local);
    }
    return local;
}

let result = use_scope(5);
print(result);

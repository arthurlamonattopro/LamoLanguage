gui_open(400, 300, "Fixture GUI");

while (gui_should_close() == 0) {
    gui_begin_frame(240, 240, 240);
    gui_draw_rect(20, 20, 160, 60, 0, 120, 215);
    gui_draw_text("GUI fixture", 40, 38, 255, 255, 255);
    gui_end_frame();
}

gui_close();

// Structs: declaration, literal, field access, field assignment
struct Point {
    x: int,
    y: int
}

let p = Point { x: 3, y: 4 };
print(p.x);
print(p.y);

p.x = 10;
p.y = 20;
print(p.x);
print(p.y);

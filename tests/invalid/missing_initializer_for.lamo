// ERRO: `for (let i = ; ...)` deve ser rejeitado com "missing initializer"
// Sprint 1 fix: o parser agora emite um erro explicito em vez de
// "invalid expression, found ;".
for (let i = ; i < 10; i++) {
    print(i);
}
